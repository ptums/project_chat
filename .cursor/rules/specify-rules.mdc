# project_chat Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-24

## Active Technologies
- Python 3.10+ + Flask, sys.stdin (standard library), existing CLI infrastructure (001-large-text-input)
- PostgreSQL (text stored in messages.content TEXT field - no size limit) (001-large-text-input)
- Python 3.10 (repo `.python-version` + venv) + psycopg2, Flask, OpenAI SDK, `requests` (new for Ollama client) (001-ollama-organizer)
- PostgreSQL (existing conversations + new `conversation_index` table) (001-ollama-organizer)
- Python 3.10+ (repo `.python-version` + venv) + psycopg2, Flask, OpenAI SDK, `requests` (existing), `pgvector` extension (PostgreSQL), OpenAI embeddings API (002-daas-semantic-retrieval)
- PostgreSQL (existing `conversation_index` table + new `embedding` column for DAAS entries) (002-daas-semantic-retrieval)
- Python 3.10+ (existing project standard) + psycopg2, python-dotenv (existing), PostgreSQL, bash (for migration scripts) (003-seamless-setup)
- PostgreSQL (existing database), SQL migration files, JSON/CSV seed data files (003-seamless-setup)
- Python 3.10+ (existing project standard) + psycopg2-binary (existing), python-dotenv (existing), PostgreSQL client tools (pg_dump, pg_restore) (001-database-backup)
- PostgreSQL database backups (SQL dump format), backup metadata files (JSON), local filesystem or network storage (001-database-backup)
- Python 3.10+ + psycopg2 (PostgreSQL), OpenAI SDK, Ollama client, python-dotenv (004-fix-conversation-saving)
- PostgreSQL (conversations, messages, conversation_index tables) (004-fix-conversation-saving)
- Python 3.10+ + psycopg2 (PostgreSQL), existing `brain_core.db` and `brain_core.config` modules (005-conversation-audit-tool)
- Python 3.10+ + openai (OpenAI API), existing `brain_core.usage_tracker` and `brain_core.chat` modules (006-fix-api-usage-and-pause-feature)
- N/A (in-memory session tracking) (006-fix-api-usage-and-pause-feature)
- Python 3.10+ (existing project standard) + Existing project dependencies (no new dependencies required) (009-refactor-script-organization)
- N/A (file system reorganization only) (009-refactor-script-organization)
- Python 3.10+ (existing project standard) + Existing dependencies (psycopg2-binary, python-dotenv, openai, requests, gitpython), subprocess (stdlib), json (stdlib), asyncio (stdlib) or threading for async operations (010-mcp-server-integration)
- N/A (MCP resources cached in memory, no persistent storage required) (010-mcp-server-integration)
- Python 3.11+ + GitPython (for committing/pushing notes), python-frontmatter (for markdown frontmatter), existing MCP server infrastructure (001-project-organized-notes)
- Local filesystem (GitLab repository clone), in-memory index (NoteIndex) (001-project-organized-notes)
- Python 3.10+ (existing project standard) + Existing dependencies (psycopg2-binary, python-dotenv, openai, requests) (011-improve-context-building)
- File system (for base system prompt file), PostgreSQL (for project_knowledge table) (011-improve-context-building)
- Python 3.10+ (existing project standard) + Existing dependencies (psycopg2-binary, python-dotenv) (012-expand-project-extension)
- PostgreSQL (project_knowledge table migration) (012-expand-project-extension)

- Python 3.10+ + Flask, psycopg2, python-dotenv, openai (Python SDK) (001-dev-environment)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.10+: Follow standard conventions

## Recent Changes
- 012-expand-project-extension: Added Python 3.10+ (existing project standard) + Existing dependencies (psycopg2-binary, python-dotenv)
- 011-improve-context-building: Added Python 3.10+ (existing project standard) + Existing dependencies (psycopg2-binary, python-dotenv, openai, requests)
- 001-project-organized-notes: Added Python 3.11+ + GitPython (for committing/pushing notes), python-frontmatter (for markdown frontmatter), existing MCP server infrastructure


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
